@echo off
REM This command is a pseudo-comment. It does nothing, so it is like a comment, and will allow you to run (viz: not run) unix⹀shell-commented lines. However, it does not syntactically operate like a comment, so other side⹀effect-producing operations that occur in the line will still occur 😬
